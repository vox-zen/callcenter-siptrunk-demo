version: "3"

services:

  asterisk:
    build: .
    container_name: asterisk-callcenter
    ports:
      - "5060:5060/udp"
      - "10000-10100:10000-10100/udp"
    volumes:
      - ../configs:/etc/asterisk
      - ../sounds:/var/lib/asterisk/sounds
